- name: Enable COPR repo for Yazi (Unofficial)
  become: true
  community.general.copr:
    state: enabled
    name: lihaohong/yazi

- name: Install yazi command line file explorer
  become: true
  ansible.builtin.package:
    name:
      - yazi
    state: present
